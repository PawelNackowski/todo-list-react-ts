import { Wrapper } from "../../Buttons/styled";
import { Spiner } from "./styled";

const Loader = () => {
  return (
    <Wrapper>
      <Spiner />
      Ładowanie
    </Wrapper>
  )
};

export default Loader;